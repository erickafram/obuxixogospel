<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Obuxixo Gospel Admin</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    <link rel="alternate icon" href="/images/favicon.svg">
    <link rel="apple-touch-icon" href="/images/favicon.svg">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Instagram Embed Script -->
    <script async src="//www.instagram.com/embed.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #F5F7FA;
            color: #333;
        }

        .dashboard-wrapper {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 240px;
            background: #1A202C;
            color: white;
            padding: 0;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            box-shadow: 2px 0 8px rgba(0, 0, 0, 0.1);
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .sidebar-header {
            padding: 18px 16px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }

        .logo-dashboard {
            font-size: 16px;
            font-weight: 700;
            background: linear-gradient(135deg, #0669de, #3d8bfd);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: -0.3px;
        }

        .sidebar-menu {
            padding: 16px 0;
        }

        .menu-section {
            margin-bottom: 20px;
        }

        .menu-section-title {
            padding: 0 16px 6px;
            font-size: 10px;
            text-transform: uppercase;
            color: #718096;
            font-weight: 500;
            letter-spacing: 0.8px;
        }

        .menu-item {
            display: flex;
            align-items: center;
            padding: 9px 16px;
            color: #A0AEC0;
            text-decoration: none;
            transition: all 0.15s;
            border-left: 2px solid transparent;
            font-size: 13px;
            font-weight: 400;
        }

        .menu-item:hover {
            background: rgba(255, 255, 255, 0.06);
            color: #E2E8F0;
            border-left-color: #0669de;
        }

        .menu-item.active {
            background: rgba(6, 105, 222, 0.12);
            color: #3d8bfd;
            border-left-color: #0669de;
            font-weight: 500;
        }

        .menu-item i {
            width: 18px;
            margin-right: 10px;
            font-size: 14px;
        }

        /* Main Content */
        .main-content {
            margin-left: 240px;
            flex: 1;
            min-height: 100vh;
        }

        /* Top Bar */
        .topbar {
            background: white;
            padding: 14px 28px;
            border-bottom: 1px solid #E2E8F0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
        }

        .mobile-menu-toggle {
            display: none;
            background: none;
            border: none;
            font-size: 24px;
            color: #1A202C;
            cursor: pointer;
            padding: 8px;
            margin-right: 12px;
            transition: all 0.2s;
        }

        .mobile-menu-toggle:hover {
            color: #0669de;
        }

        .mobile-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .mobile-overlay.active {
            display: block;
            opacity: 1;
        }

        .topbar-left h2 {
            font-size: 20px;
            font-weight: 600;
            color: #1A202C;
            letter-spacing: -0.3px;
        }

        .topbar-right {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 6px 12px;
            background: #F7FAFC;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.15s;
            border: 1px solid #E2E8F0;
        }

        .user-menu:hover {
            background: #EDF2F7;
            border-color: #CBD5E0;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: linear-gradient(135deg, #0669de, #3d8bfd);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 13px;
        }

        .user-info {
            display: flex;
            flex-direction: column;
        }

        .user-name {
            font-size: 13px;
            font-weight: 500;
            color: #2D3748;
        }

        .user-role {
            font-size: 11px;
            color: #718096;
        }

        .btn-logout {
            padding: 7px 14px;
            background: linear-gradient(135deg, #0669de, #3d8bfd);
            color: white;
            border: none;
            border-radius: 7px;
            font-size: 12px;
            font-weight: 500;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            transition: all 0.15s;
            box-shadow: 0 2px 6px rgba(6, 105, 222, 0.2);
        }

        .btn-logout:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(6, 105, 222, 0.3);
        }

        .btn-logout i {
            font-size: 12px;
        }

        /* Notificações */
        .notification-bell {
            position: relative;
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s;
            margin-right: 16px;
            border: 1px solid #E2E8F0;
        }

        .notification-bell:hover {
            background: #F7FAFC;
            border-color: #CBD5E0;
        }

        .notification-bell i {
            font-size: 18px;
            color: #4A5568;
        }

        .notification-badge {
            position: absolute;
            top: -4px;
            right: -4px;
            background: #EF4444;
            color: white;
            font-size: 10px;
            font-weight: 600;
            padding: 2px 6px;
            border-radius: 10px;
            min-width: 18px;
            text-align: center;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        .notification-panel {
            position: absolute;
            top: 70px;
            right: 20px;
            width: 420px;
            max-height: 600px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            border: 1px solid #E2E8F0;
            z-index: 1000;
            overflow: hidden;
        }

        .notification-header {
            padding: 16px 20px;
            border-bottom: 1px solid #E2E8F0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: #F7FAFC;
        }

        .notification-header h3 {
            font-size: 16px;
            font-weight: 600;
            color: #1A202C;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .notification-header h3 i {
            color: #0669de;
        }

        .btn-mark-all {
            background: transparent;
            border: none;
            color: #3B82F6;
            font-size: 12px;
            cursor: pointer;
            padding: 6px 10px;
            border-radius: 6px;
            transition: all 0.2s;
            font-weight: 500;
        }

        .btn-mark-all:hover {
            background: #EBF5FF;
        }

        .notification-list {
            max-height: 500px;
            overflow-y: auto;
        }

        .notification-item {
            padding: 16px 20px;
            border-bottom: 1px solid #F1F5F9;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            gap: 12px;
            align-items: start;
        }

        .notification-item:hover {
            background: #F8FAFC;
        }

        .notification-item.unread {
            background: #F0F9FF;
            border-left: 3px solid #3B82F6;
        }

        .notification-icon {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            font-size: 18px;
        }

        .notification-icon.new-post {
            background: #DBEAFE;
            color: #3B82F6;
        }

        .notification-icon.error {
            background: #FEE2E2;
            color: #EF4444;
        }

        .notification-content {
            flex: 1;
        }

        .notification-title {
            font-size: 14px;
            font-weight: 600;
            color: #1A202C;
            margin-bottom: 4px;
        }

        .notification-message {
            font-size: 13px;
            color: #64748B;
            margin-bottom: 6px;
        }

        .notification-time {
            font-size: 11px;
            color: #94A3B8;
        }

        .notification-loading {
            padding: 40px;
            text-align: center;
            color: #94A3B8;
        }

        .notification-empty {
            padding: 60px 20px;
            text-align: center;
            color: #94A3B8;
        }

        .notification-empty i {
            font-size: 48px;
            margin-bottom: 16px;
            opacity: 0.3;
        }

        /* Dashboard Content */
        .dashboard-content {
            padding: 28px;
        }

        /* Scrollbar Sidebar */
        .sidebar::-webkit-scrollbar {
            width: 6px;
        }

        .sidebar::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
        }

        .sidebar::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 3px;
        }

        .sidebar::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        .welcome-section {
            margin-bottom: 28px;
            padding-bottom: 20px;
            border-bottom: 1px solid #E2E8F0;
        }

        .welcome-section h1 {
            font-size: 24px;
            font-weight: 600;
            color: #1A202C;
            margin-bottom: 6px;
            letter-spacing: -0.5px;
        }

        .welcome-section p {
            font-size: 14px;
            color: #718096;
            margin: 0;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 18px;
            margin-bottom: 28px;
        }

        .stat-card {
            background: white;
            padding: 18px;
            border-radius: 12px;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
            border: 1px solid #E8E8E8;
            display: flex;
            align-items: center;
            gap: 14px;
            transition: all 0.15s;
        }

        .stat-card:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
            border-color: #CBD5E0;
        }

        .stat-icon {
            width: 48px;
            height: 48px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: white;
            flex-shrink: 0;
        }

        .stat-content h3 {
            font-size: 11px;
            color: #718096;
            font-weight: 500;
            margin-bottom: 4px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .stat-number {
            font-size: 24px;
            font-weight: 700;
            color: #1A202C;
            margin: 0;
        }

        /* Quick Actions */
        .quick-actions {
            margin-top: 28px;
        }

        .quick-actions h2 {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 14px;
            color: #1A202C;
            letter-spacing: -0.3px;
        }

        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 14px;
        }

        .action-card {
            background: white;
            padding: 20px 16px;
            border-radius: 12px;
            box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
            border: 1px solid #E8E8E8;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            text-decoration: none;
            color: #2D3748;
            transition: all 0.15s;
        }

        .action-card:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 10px rgba(6, 105, 222, 0.15);
            border-color: #0669de;
            color: #0669de;
        }

        .action-card i {
            font-size: 28px;
            color: #0669de;
            transition: all 0.15s;
        }

        .action-card:hover i {
            transform: scale(1.1);
        }

        .action-card span {
            font-size: 13px;
            font-weight: 500;
        }

        /* Responsividade */
        @media (max-width: 1024px) {
            .sidebar {
                width: 220px;
            }
            .main-content {
                margin-left: 220px;
            }
            .topbar {
                padding: 12px 20px;
            }
            .dashboard-content {
                padding: 20px;
            }
            .stats-grid {
                grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
                gap: 14px;
            }
            .actions-grid {
                grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            }
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
            .sidebar.active {
                transform: translateX(0);
            }
            .main-content {
                margin-left: 0;
                width: 100%;
            }
            .mobile-menu-toggle {
                display: block;
            }
            .topbar {
                padding: 10px 16px;
            }
            .topbar-left {
                display: flex;
                align-items: center;
            }
            .topbar-left h2 {
                font-size: 16px;
            }
            .topbar-right {
                gap: 8px;
            }
            .user-info {
                display: none;
            }
            .btn-logout span {
                display: none;
            }
            .btn-logout {
                padding: 8px 10px;
            }
            .notification-bell {
                width: 36px;
                height: 36px;
                margin-right: 0;
            }
            .notification-panel {
                width: calc(100vw - 32px);
                right: 16px;
                left: 16px;
                max-height: 70vh;
            }
            .dashboard-content {
                padding: 16px;
            }
            .welcome-section h1 {
                font-size: 20px;
            }
            .welcome-section p {
                font-size: 13px;
            }
            .stats-grid {
                grid-template-columns: 1fr;
                gap: 12px;
            }
            .stat-card {
                padding: 16px;
            }
            .stat-icon {
                width: 44px;
                height: 44px;
                font-size: 18px;
            }
            .stat-number {
                font-size: 20px;
            }
            .actions-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 12px;
            }
            .action-card {
                padding: 16px 12px;
            }
            .action-card i {
                font-size: 24px;
            }
            .action-card span {
                font-size: 12px;
            }
        }

        @media (max-width: 480px) {
            .topbar-left h2 {
                font-size: 14px;
            }
            .user-avatar {
                width: 28px;
                height: 28px;
                font-size: 12px;
            }
            .btn-logout {
                padding: 6px 8px;
            }
            .btn-logout i {
                font-size: 14px;
            }
            .dashboard-content {
                padding: 12px;
            }
            .stats-grid {
                gap: 10px;
            }
            .stat-card {
                padding: 14px;
                gap: 12px;
            }
            .stat-icon {
                width: 40px;
                height: 40px;
                font-size: 16px;
            }
            .stat-content h3 {
                font-size: 10px;
            }
            .stat-number {
                font-size: 18px;
            }
            .actions-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-wrapper">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo-dashboard">obuxixo gospel</div>
            </div>
            
            <nav class="sidebar-menu">
                <div class="menu-section">
                    <div class="menu-section-title">Principal</div>
                    <a href="/dashboard" class="menu-item active">
                        <i class="fas fa-home"></i>
                        <span>Dashboard</span>
                    </a>
                    <a href="/" class="menu-item" target="_blank">
                        <i class="fas fa-globe"></i>
                        <span>Ver Site</span>
                    </a>
                </div>

                <div class="menu-section">
                    <div class="menu-section-title">Conteúdo</div>
                    <a href="/dashboard/posts" class="menu-item">
                        <i class="fas fa-newspaper"></i>
                        <span>Posts</span>
                    </a>
                    <a href="/dashboard/comentarios" class="menu-item">
                        <i class="fas fa-comments"></i>
                        <span>Comentários</span>
                    </a>
                    <a href="/dashboard/categorias" class="menu-item">
                        <i class="fas fa-folder"></i>
                        <span>Categorias</span>
                    </a>
                </div>

                <div class="menu-section">
                    <div class="menu-section-title">Inteligência Artificial</div>
                    <a href="/dashboard/ia/lote" class="menu-item">
                        <i class="fas fa-robot"></i>
                        <span>Geração Automática</span>
                    </a>
                </div>

                <% if (user.role === 'admin') { %>
                <div class="menu-section">
                    <div class="menu-section-title">Sistema</div>
                    <a href="/dashboard/usuarios" class="menu-item">
                        <i class="fas fa-users"></i>
                        <span>Usuários</span>
                    </a>
                    <a href="/dashboard/paginas" class="menu-item">
                        <i class="fas fa-file-alt"></i>
                        <span>Páginas</span>
                    </a>
                    <a href="/dashboard/configuracoes" class="menu-item">
                        <i class="fas fa-cog"></i>
                        <span>Configurações</span>
                    </a>
                </div>
                <% } %>
            </nav>
        </aside>

        <!-- Main Content -->
        <div class="main-content">
            <!-- Top Bar -->
            <div class="topbar">
                <div class="topbar-left">
                    <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle menu">
                        <i class="fas fa-bars"></i>
                    </button>
                    <h2><%= title %></h2>
                </div>
                <div class="topbar-right">
                    <!-- Notificações -->
                    <div class="notification-bell" id="notificationBell" onclick="toggleNotifications()">
                        <i class="fas fa-bell"></i>
                        <span class="notification-badge" id="notificationBadge" style="display: none;">0</span>
                    </div>
                    
                    <div class="user-menu">
                        <div class="user-avatar"><%= user.nome.charAt(0).toUpperCase() %></div>
                        <div class="user-info">
                            <div class="user-name"><%= user.nome %></div>
                            <div class="user-role"><%= user.role %></div>
                        </div>
                    </div>
                    <a href="/logout" class="btn-logout">
                        <i class="fas fa-sign-out-alt"></i> <span>Sair</span>
                    </a>
                </div>
                <div class="mobile-overlay" id="mobileOverlay"></div>
                
                <!-- Painel de Notificações -->
                <div class="notification-panel" id="notificationPanel" style="display: none;">
                    <div class="notification-header">
                        <h3><i class="fas fa-bell"></i> Notificações</h3>
                        <button onclick="markAllAsRead()" class="btn-mark-all">
                            <i class="fas fa-check-double"></i> Marcar todas como lidas
                        </button>
                    </div>
                    <div class="notification-list" id="notificationList">
                        <div class="notification-loading">
                            <i class="fas fa-spinner fa-spin"></i> Carregando...
                        </div>
                    </div>
                </div>
            </div>
